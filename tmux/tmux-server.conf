# tmux settings for server machines

# Source common settings
source-file ~/usr/dotfiles/public/tmux/tmux-common.conf

# Colors for the status bar: blue bg, white fb and black window selector
set -g status-bg blue
set -g status-fg white
set-window-option -g window-status-current-bg white
