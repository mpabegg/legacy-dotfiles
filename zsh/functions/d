# Quickly change to frequently used directories

local -A aliases
aliases=(
    d        "$PROJECTS_DIR/jigsaw"
    di       "$PROJECTS_DIR/jigsaw-infra"
    dot      "$DOTFILES_DIR"
)

cd ${aliases[${1:-d}]}
pwd
