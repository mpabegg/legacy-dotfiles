# Quickly change to frequently used directories

local -A aliases
aliases=(
    d        "$PROJECTS_DIR"
    df       "$DOTFILES_DIR"
    b        "$PROJECTS_DIR/babbel"
    db       "$HOME/Dropbox"
    sbl      "$HOME/Dropbox/Bass Archives/ScottBassLessons"
)

cd ${aliases[${1:-d}]}
pwd
