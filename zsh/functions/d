# Quickly change to frequently used directories

local -A aliases
aliases=(
    d        "$PROJECTS_DIR"
    df       "$DOTFILES_DIR"
    mc       "$PROJECTS_DIR/mediacomments"
    fcc      "$PROJECTS_DIR/freecodecamp"
)

cd ${aliases[${1:-d}]}
pwd
